name: Meta-learning scripts

on:
  push:
    branches: [main]
    paths:
      - "test_learning.jl"
      - "test_escalabilidad.jl"
      - "workflow_rsi_update.jl"
      - "metalearning_integration.jl"
      - "meta_learning_tep.jl"
      - "integrar_aprendizaje_8d.jl"
      - "Ecosistema.jl"
      - "soberania_absoluta.jl"
      - "Project.toml"
      - "Manifest.toml"
      - ".github/workflows/meta_learning.yml"
  pull_request:
    paths:
      - "test_learning.jl"
      - "test_escalabilidad.jl"
      - "workflow_rsi_update.jl"
      - "metalearning_integration.jl"
      - "meta_learning_tep.jl"
      - "integrar_aprendizaje_8d.jl"
      - "Ecosistema.jl"
      - "soberania_absoluta.jl"
      - "Project.toml"
      - "Manifest.toml"
      - ".github/workflows/meta_learning.yml"
  workflow_dispatch:

concurrency:
  group: meta-learning-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  run-meta-learning-scripts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Julia
        uses: julia-actions/setup-julia@v2
        with:
          version: "1.10"
          arch: x64

      - name: Cache Julia packages
        uses: julia-actions/cache@v2

      - name: Install dependencies
        run: |
          julia --project=. -e 'using Pkg; Pkg.instantiate()'

      - name: Run meta-learning scripts
        run: |
          julia --project=. test_learning.jl
          julia --project=. test_escalabilidad.jl
          julia --project=. workflow_rsi_update.jl
          julia --project=. metalearning_integration.jl
          julia --project=. meta_learning_tep.jl
          julia --project=. integrar_aprendizaje_8d.jl
