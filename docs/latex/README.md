# LaTeX PDF source bundle

This folder contains a printable LaTeX manuscript source that mirrors the
Markdown and Documenter documentation content for the repository.

## Main source

- `quantum_api_mermin_perez.tex`

## Intended workflow

Use a LaTeX compiler such as `pdflatex` or `latexmk` to generate a PDF from
this source file.
